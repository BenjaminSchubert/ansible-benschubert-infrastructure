---
ingress_networks:
  - ingress-auth
  - ingress-monitoring

ingress_network_aliases:
  - "{{ auth_authentik_hostname }}"
  - "{{ monitoring_loki_hostname }}"
  - "{{ monitoring_mimir_hostname }}"

ingress_additional_networks: []

default_monitor_agent_user_group: null
auth_monitor_agent_user_group: "{{ default_monitor_agent_user_group }}"
monitoring_monitor_agent_user_group: "{{ default_monitor_agent_user_group }}"
ingress_monitor_agent_user_group: "{{ default_monitor_agent_user_group }}"
