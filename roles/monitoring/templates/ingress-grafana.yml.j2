{{ template_warning }}
---
http:
  routers:
    grafana:
      rule: Host(`{{ hostname }}`)
      service: grafana
      priority: 10

  services:
    grafana:
      loadBalancer:
        servers:
          - url: http://monitoring-grafana:3000
