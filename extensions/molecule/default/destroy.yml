---
- name: Destroy container instances
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Destroy Virtual machine
      ansible.builtin.command:
        cmd: vagrant destroy --force
      register: _vagrant_destroy
      changed_when: "'Domain is not created' in _vagrant_destroy.stdout"
